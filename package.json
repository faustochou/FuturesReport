{
  "name": "futuresreport",
  "version": "0.1.0",
  "description": "FuturesReport - 未來學 AI 多智能體模擬引擎",
  "scripts": {
    "setup": "npm install && cd frontend && npm install",
    "setup:backend": "cd backend && uv sync",
    "setup:all": "npm run setup && npm run setup:backend",
    "start": "npm run dev",
    "dev": "concurrently --kill-others -n \"backend,frontend\" -c \"green,cyan\" \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && (uv run python run.py || .venv\\Scripts\\python run.py || .venv/bin/python run.py)",
    "frontend": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "AGPL-3.0",
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
